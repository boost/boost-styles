# frozen_string_literal: true

RSpec.describe Boost::Styles do
  it 'has a version number' do
    expect(Boost::Styles::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
